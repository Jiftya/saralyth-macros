# Seralyth Macros

I friggin spent hours freaking on this

## Folder structure

just look at the folders broski

## Notes

- `logo.png` is used as the favicon from `assets/logo.png`
- The page loads `data/posts.json` locally when the remote list is unavailable :)
- The ui logic is in `js/app.js`
- peak styles r in `css/styles.css`

- odds the freaking goat, yo